const themesList = document.getElementById("gkthemes-grid");

// - Generate System Themes upon load
// - Assign No Theme button upon load
// - Get list of chrThemes
// - Get list of lwThemes
// - Assemble chrThemes
// - Assemble lwThemes
//  - Assign Light and Dark events to the top-area radios
// - Sort them based on data-index
// - Place them!

async function populateThemesList() {

}

async function selectTheme() {
    // Selects theme from the top, and then a Chromium Theme, Firefox Theme or Default Theme depending on criteria
    //  Mark Default Theme as fallback based on which theme is currently Default Theme
    //  Chromium Theme is checked for if LWTheme is Light
    //  Default Theme is selected if LWTheme is System Theme

    
}